<%= form_with(model: to_arduino, local: true) do |form| %>
  <% if to_arduino.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(to_arduino.errors.count, "error") %> prohibited this to_arduino from being saved:</h2>

      <ul>
        <% to_arduino.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
